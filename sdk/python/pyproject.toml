[project]
name = "sst"
version = "0.2.0"
description = "SST SDK"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["pycryptodome>=3.20.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
